awk 'BEGIN {
    FS = ","
}

{
    
    for (i = 1; i <= NF; i++) {
          
         
        if (substr($i, 3, 1) == "=") {
    len = length($i)
    bob = index($i, "=")
    print($i = substr($i, bob +1 , len-bob))    # Get text after equal sign / empty set
}
    }
}' data1.txt
